Valutazione backend.

## Voto complessivo: 6/10

---

### PUNTI DI FORZA (+)

| Criterio | Voto | Note |
|---|---|---|
| Architettura a strati | 7 | Controller Service Repository pulito |
| Security/Auth | 7 | Keycloak + JWT + @PreAuthorize + @Version ottimistico |
| Exception handling | 7 | GlobalExceptionHandler + ApiBankException + i18n message source |
| DTO usage | 7 | Entità non esposte direttamente |
| Lombok | 8 | Boilerplate ridotto |
| Audit logging | 7 | AuditLogService presente |
| DataInitializer | 8 | Seed data automatizzato |
| Swagger | 7 | OpenAPI integrato |

### PUNTI DEBOLI (-)

| Criterio | Voto | Problemi |
|---|---|---|
| Test | 2 | Solo 3 file test (AccountLimit, Beneficiary, Transaction). 0 test per auth, admin, user, card, etc. |
| i18n lacunosa | 4 | EmployeeUserController e EmployeeCardController ritornano stringhe italiane hardcoded: Registrazione validata, conto attivato. / Carta bloccata con successo.  zero i18n. AccountService.getStatusName() + CustomerProfileController ritornano INATTIVO, ATTIVO hardcoded. Alert @Adult + CreateEmployeeRequestDto hanno messaggi in italiano. |
| N+1 queries | 3 | AdminTransactionController.findAll() chiama findById() in loop per tipo/stato di ogni transazione |
| DTO in controller | 4 | AdminTransactionListItemDto = classe interna del controller |
| API URL inconsistente | 4 | /api/v1/... vs /pin/setup vs /api/users/export vs /api/cards/... |
| Nessuna paginazione | 4 | Admin lists (clienti, impiegati, conti) ritornano tutto in unbounded list |
| God Class | 4 | AccountService fa tutto: operazioni cliente, dipendente, chiusure, freeze, status |
| Business logic in controller | 5 | AuthController.keycloakLogin() ha logica di stato utente, JWT parsing, user sync |
| Rate limiting | 1 | Nessuna protezione su login/register |
| Caching | 2 | Nessun @Cacheable per lookup data (tipi transazione, status) |
| RuntimeException in IbanGenerator | 5 | Dovrebbe lanciare ApiBankException |
| CORS sparso | 5 | @CrossOrigin sui controller invece di configurazione globale |
| SecurityUtil statico | 5 | Non mockabile nei test |
| Profile pics su filesystem | 5 | Nessun cleanup, nessun object storage |

---

### RIEPILOGO PER MACRO-AREA

| Area | Voto | Giudizio |
|---|---|---|
| Architettura | 7/10 | Solida impalcatura a strati, ma alcune responsabilità contorte (AccountService, AuthController) |
| Codice | 5/10 | Metà file puliti, metà con stringhe italiane, N+1, DTO annidati |
| Testing | 2/10 | Insufficiente per un sistema bancario |
| Performance | 5/10 | N+1, no cache, no paginazione admin |
| Manutenibilità | 6/10 | Buona struttura base ma i18n incompleta e inconsistenza url creano debito tecnico |

### COSA ABBASSA IL VOTO (quick win per salire a 7+)

1. i18n completa: EmployeeUserController (4 stringhe), EmployeeCardController (2 stringhe), AccountService.getStatusName(), CustomerProfileController status  usare error code o MessageSource
2. N+1 fix: AdminTransactionController  join query con @EntityGraph o fetch join
3. Paginazione: Admin endpoint per clienti/impiegati/conti
4. Test: Almeno service layer tests per auth flow, card flow, employee flow
5. DTO separati: Estrarre AdminTransactionListItemDto in file proprio
6. Uniformare URL: Tutto sotto /api/v1/
7. Rate limiting: Spring Boot + bucket4j o similare su /auth/**
